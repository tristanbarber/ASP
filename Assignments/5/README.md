# Assignment 5
## Tristan Barber
### To build - run "make" with no arguments.
### Note that there are optional build params majorno (default 500), numdevices (default 3), and size (default 16*PAGE_SIZE)